"/home/Matt/Game Development/Extracted game data/Star Wars Jedi Knight/JK level 3DOs/m10.3do"

You can use the string generated by UniversalMesh.materials to open all the JK materials in JKPaint.
- Well, first open one and load the correct palette, then load all the rest.
- Then you just have to go through them like File -> Save Bitmap, File -> Close, etc. 



TODO

IN TRIANGLUATION METHOD, CHECK TO SEE IF ANY OTHER VERTICES INTERSECT THE TRIANGLE



Import BMPs
Convert and output BMPs
Assign palette.
Maybe just record all the palettes manually, maybe have a thing to import palettes.
Write .map files.




test.3do => .map
*	Trianglified/non-trianglified, with or without swapped YZ coordinates:
*	>>>	reverseVertexOrder = false; invertFaceNormals = false; //Cube is inside-out.
*	*	reverseVertexOrder = true; invertFaceNormals = false; //Brushes are empty.
*	>>>	reverseVertexOrder = true; invertFaceNormals = true; //Cube is outside-out.
*	*	reverseVertexOrder = false; invertFaceNormals = true; //Brushes are empty.




.map textures

* Are projected along one of the 3 axes.
* If 45 degrees:
	y-axis takes precedent over x-axis.
	z-axis takes precedent over y-axis and x-axis.

So we get an UniversalMesh from the 3do, during which we adjust the textureVertices by 1/textureSize.